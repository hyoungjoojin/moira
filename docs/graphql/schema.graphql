type Query {
  user(id: String!): User
}

type User {
  id: ID!
  email: String!
  createdAt: DateTime!
}

scalar DateTime

type Mutation {
  register(input: RegisterUserInput): User
}

input RegisterUserInput {
  email: String!
}